// Crie um array let números = [5, 10, 15] e use um loop for para exibir cada número no console.

let numeros = [5, 10, 15];

numeros.forEach(number => {
    console.log(number);
});

for (let i = 0; i < numeros.length; i++) {
    console.log(numeros[i]);
}